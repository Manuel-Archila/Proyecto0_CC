(* COMENTARIO *)
class Main {
    a : Int <- 0;
    b: Silly <- new Silly;
    
    main() : Int {
        otra(1)
    };

    k : Int <- a;

    otra(z: Int): Int {
        let x: Int<-89 in (
            let y: Int<-x, r:Int <- 2, k:Bool <- false in {
                k <-1 + z;
            }
        )
    };
};

class Silly {
    x:Int <- 1;
};